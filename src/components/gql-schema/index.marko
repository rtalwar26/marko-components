<div>
    <if(state.status=="success" )>
        <${input.then.renderBody} fields=state.fields />
        </if>
        <if(state.status=="error" )>
            <${input.catch.renderBody} error=state.error />
            </if>
</div>